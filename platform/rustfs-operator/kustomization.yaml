apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
helmCharts:
  - name: rustfs-operator
    repo: https://operator.rustfs.com
    version: 0.0.5
    releaseName: rustfs-operator
    namespace: rustfs-system
    valuesFile: values.yaml
